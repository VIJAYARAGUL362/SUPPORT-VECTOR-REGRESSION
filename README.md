# SUPPORT-VECTOR-REGRESSION
Support Vector Regression (SVR) is a regression technique that uses the principles of Support Vector Machines to predict continuous values. It finds a hyperplane that fits the data while maintaining a margin of tolerance (epsilon), making it robust against outliers. SVR effectively captures complex relationships in high-dimensional datasets.
